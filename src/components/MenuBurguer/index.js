import React from 'react';

import { MenuWrapper, MenuIcon } from './styled';

const MenuBurguer = () => {
  return (
    <MenuWrapper>
      <MenuIcon />
    </MenuWrapper>
  );
}

export default MenuBurguer;
